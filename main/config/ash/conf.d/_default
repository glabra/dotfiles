# no persistence history
unset HISTFILE

# disable ^S
stty stop undef

# for delete key in st
tput smkx

# set PS1
# callback: ps1_after_path
PS1='$(tput bold)$(basename "${PWD}")$(tput sgr0)$(execute_callback ps1_after_path)$(tput bold)\$$(tput sgr0) '
