detect_less_variant () {
    is_busybox_binary 'less' && \
        printf '%s' 'BusyBox' || \
        printf '%s' 'Greenwood'
}

case "$(detect_less_variant)" in
    'Greenwood') alias less='less -R' ;;
    *) ;;
esac

unset -f detect_less_variant

alias le=less

