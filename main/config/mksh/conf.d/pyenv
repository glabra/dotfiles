# lazy loading
pyenv() {
    export PYENV_ROOT="${pyenv}"
    export PATH="${PYENV_ROOT}/bin:${PATH}"
    unset pyenv
    unset -f pyenv
    eval "$(pyenv init -)"
    eval "$(pyenv virtualenv-init -)"
    pyenv "$@"
}
