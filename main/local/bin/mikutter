#!/bin/sh
# wrapper for mikutter under rbenv environment
set -u
set -e
IFS="	 
"
export PATH="${HOME}/.rbenv/bin:/bin:/usr/bin"
export LANG="ja_JP.UTF-8"
eval "$(rbenv init -)"

cd "${HOME}/.mikutter/core/"
rbenv exec ruby ./mikutter.rb &
