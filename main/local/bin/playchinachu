#!/bin/sh
set -ue
umask 0027
export PATH='/bin:/usr/bin'
export LANG='C'
IFS=$' \t\n'

[[ -z "$1" ]] && exit 1
type 'ffplay' > /dev/null 2>&1 || exit 1

url=$(cat "$1" | awk '/<location>/{gsub("</?location>",""); print $0;}' | head -n 1)
curl -su akari:bakuhatsu "$url" | ffplay -vf yadif -
